
通过引入mybatis-spring-boot-starter,引入自动配置类 MybatisAutoConfiguration

1.自动装载，通过spring.factories

2.引入bd

checkConfigFileExists 创建类前验证配置文件是否存在

org.mybatis.spring.boot.autoconfigure.MybatisAutoConfiguration.AutoConfiguredMapperScannerRegistrar.registerBeanDefinitions

①类中有个MybatisProperties类，该类对应的是mybatis的配置文件

②创建SqlSessionFactory类、Configuration类（mybatis最主要的类，保存着与mybatis相关的东西）

③SqlSessionTemplate(实现接口SqlSession)，作用是与mapperProoxy代理类有关

 2.1 注入bd[MapperScannerConfigurer],比较重要的属性:扫码包basePackage，注解过滤器只识别包含Mapper的注解
将接口作为factorybean[MapperFactoryBean]注入到spring容器中
org.mybatis.spring.mapper.MapperScannerConfigurer#postProcessBeanDefinitionRegistry